const { data, error } = await supabaseAdmin.rpc('solicita_crear_orden_distribucion', {
  p_vendedor_id: uuid_vendedor, 
  p_chofer_id: uuid_chofer,
  p_productos_json: lista_productos_array // Arreglo de objetos con llaves: producto_id, cantidad, precio_unitario
});