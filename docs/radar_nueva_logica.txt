Cambios en el Radar.
Se debe crear una tabla maestra para el control de los radares, tabla puede llamarse "radars"
Los campos que debe tener la tabla son los siguientes:
radar_id UUID
despachador_id
fecha_despacho DATE
total_cantidad_solicitada numeric sin decimales
total_cantidad_despachada numeric sin decimales
total_contenedores_retirados numeric sin decimales
status_radar boolean (.T. si el despacho salió asi no sea completo el mismo, .F. si no salió el despacho)
created_at timestamptz

Se me ocurre la idea de crear el campo radar_id FK en detalle_distribucion para identificarlo e incluirlo en el radar.
La intención es que desde el Front se pueda imprimir el radar (reporte) al despachador asignado, el reporte debe tener los siguientes datos:
radar_id
fecha_despacho
despachador_id
perfiles_usuario.nombre_completo
ordenes_distribucion.id
detalle_distribucion.producto_id
productos.nombre
detalle_distribucion.cantidad_solicitada
detalle_distribucion.cantidad_despachada (obviamente sin información)
detalle_distribucion.motivo_rechazo (obviamente sin información)
detalle_distribucion.contenedores_retirados (obviamente sin información)

Importante, es posible que una ordenes_distribucion.id se cambie a un siguiente radar, porque no se pudo despachar ese día al igual que el campo radar_id FK en detalle_distribucion
se debe permitir ese cambio.

El Front debe mostrar electronicamente el radar para que sea editado por el despachador. los campos que el despachador puede editar son:
cantidad_despachada
motivo_rechazo
contenedores_retirados

Creo que eso depende del front y no de nosotros que somos la capa de datos.

No quise usar tabla hija, creo que con las relaciones se pueden capturar los datos necesitados.
Tienes permiso para asesorarme según tu experiencia

Puntos importantes:
la fecha_despacho viene de la tabla ordenes_distribucion.fecha_despacho.
El PROCEDURE guardar_resultado_despacho_radar.sql debe llenar las tablas involucradas: detalle_distribucion y movimientos_contenedores (cantidad_entregada, cantidad_retirada)
La cantidad entregada es automática si el producto tiene asignado un contenedor y es un contenedor por producto.
Lo tienes contemplado así?

Vamos agregar un nuevo campo a la tabla radars titulado aprobado tipo bollean, porque cuando el despachador entrege cuentas al gerente este gerente es quien aprueba
y al aprobar se ejecutan los STORE PROCEDURE.

## Functions lista de radars según fecha
Vas a crear una Functions Retorna_lista_radars_segun_rango_fechas(tn_idDespachador, tdFechaInicial, tdFechaLimite)
para retornar la lista de radars ordenado por fechas descendentes según rango de fechas pasados desde el backend como parametros 
Para que el front pueda mostrar algo así:
------------------------------------------------------
Fecha       ID Radar 	Total paradas  Items   SKU
------------------------------------------------------
25/08/2026    #48			12			320		2 
24/08/2026    #47			12			405		5
23/08/2026    #46			15			520		3
------------------------------------------------------

Vas a crear una Funtions que retorne un detalle resumido de las ordenes contenidas en un id_radar que se te pasará como parámetro
FUNCTION Retorna_Ordenes_Distribucion_Segun_IdRadar(tnIdRadar)

Debe retornar:
id_orden_distribucion
Ruta
razon_social
direccion_fiscal
Items
SKU 
contenedores_retirados

Nota: SKU es la cantidad de id_producto despachados

Has el análisis, crea el IMPLEMENTATION PLAN en base a lo expuesto anteriormente.

## Despacho de productos no es parcial
Si el front envía que las cantidades despachadas están en cero, la orden queda bajo el status de 'devuelta', sin embargo, igualmente 
se debe permitir registrar los contenedores registrados

## FUNCTIONS solicita_aprobar_radar(tnIdRadar)
por defecto el campo status_radar debe ser .F. (no aprobado)
esta función cambia el status a .T. (aprobado)
Ejecuta las funciones referente a:
Actualiza el saldo de los contenedores a los clientes
Devuelve el inventario no despachado del camion (inventario movil) al almacén. 

## FUNCTIONS retorna_inventario_no_despachado_para_almacen()
Se debe crear una Function para retornar el inventario no despachado al almacen y esto se hace con las ordenes cuyo estado sea 'devuelto'
Tienes libertad para asesorarme de cual sería la lógica, porque un radar puede tener mas de una orden de distribución no despachadas
y esta función se ejecuta cuando el radar es aprobado por el usuario gerente 

